AppBundle\Entity\Ministry:
    type: entity
    table: ministry
    id:
        id:
            type: integer
            id: true
            generator:
                strategy: AUTO
    fields:
        name:
            type: string
            length: '40'
        description:
            type: text
            nullable: true
        position:
            type: smallint
            nullable: true
    manyToOne:
        category:
            targetEntity: AppBundle\Entity\Ministry\Category
            inversedBy: ministries
            joinColumn:
                name: category_id
                nullable: false
                onDelete: CASCADE
    oneToMany:
        responsibleAssignments:
            targetEntity: AppBundle\Entity\Ministry\ResponsibleAssignment
            cascade: [persist, remove]
            mappedBy: ministry
    lifecycleCallbacks: {  }
