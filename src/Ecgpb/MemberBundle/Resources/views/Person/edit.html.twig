{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Person edit</h1>

    {{ form(edit_form) }}

    <ul class="record_actions">
        <li>
            <a href="{{ path('ecgpb.member.person.index') }}">
                Back to the list
            </a>
        </li>
    </ul>
{% endblock %}
